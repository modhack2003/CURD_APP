const config = {
  purge: ['./src/**/*.{js,jsx,ts,tsx}', './public/index.html'],
  darkMode: 'class', // or 'media' or 'class'
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
